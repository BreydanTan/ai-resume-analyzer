# AI 简历分析系统 - 技术规格文档（中文版）

**文档版本**: 1.0
**最后更新**: 2025-11-16
**适用人群**: 开发者、系统管理员、产品经理
**语言**: 中文（简化为日常用语）

---

## 目录

1. [项目概述](#项目概述)
2. [技术架构](#技术架构)
3. [数据库设计](#数据库设计)
4. [核心业务流程](#核心业务流程)
5. [前后端交互](#前后端交互)
6. [API接口文档](#api接口文档)
7. [环境配置指南](#环境配置指南)
8. [二次开发指南](#二次开发指南)
9. [部署指南](#部署指南)
10. [附录](#附录)

---

## 1. 项目概述

### 1.1 项目简介（日常语言）

想象你有一份简历要投给Google，但你不确定这份简历是否"合格"。这个项目就像一个专业的简历评审官，它可以：

1. **接收你的简历**（上传PDF文件）
2. **分析简历内容**（使用AI检查内容质量）
3. **给出评分**（0-100分，告诉你有多少"命中率"）
4. **提供改进建议**（告诉你哪里需要改进）

**核心功能列表**：
- 🔐 **用户认证** - 像银行一样，你需要登录才能使用
- 📄 **简历上传** - 支持上传PDF格式的简历
- 🤖 **AI分析** - 使用最新的AI技术（Claude）分析你的简历
- 📊 **评分系统** - 从5个维度评估（ATS兼容性、语气风格、内容质量、结构组织、技能匹配）
- 💡 **智能建议** - 列举优点和改进点
- 💾 **数据保存** - 所有简历自动保存，下次可以查看

### 1.2 技术亮点

1. **零服务器架构** - 不需要购买和管理传统的服务器，像用云存储一样简单
2. **云端存储** - 简历和数据都安全地存储在云端（Puter平台）
3. **多模态AI** - AI可以理解文本和图片，从多个角度分析简历
4. **实时反馈** - 上传简历后几秒钟就能获得AI的反馈
5. **用户隐私** - 数据只属于你，平台无法访问

### 1.3 适用场景

✅ **最适合**：
- 求职者想要快速获得简历反馈
- HR想要批量评估简历质量
- 学生准备校招面试
- 职业转换者想要优化简历

❌ **不太适合**：
- 需要人工审核和修改的场景
- 需要持久化存储超过90天的场景（依赖Puter账户）

---

## 2. 技术架构

### 2.1 整体架构（用外卖平台类比）

想象这个系统像一个外卖平台：

```
┌─────────────────────────────────────────────────────┐
│          你的手机（浏览器）                           │
│  ┌─────────────────────────────────────────────┐   │
│  │      应用程序（React）                       │   │
│  │  - 显示简历列表（像订单列表）                │   │
│  │  - 上传简历页面（像下单页面）                │   │
│  │  - 查看分析结果（像查看配送进度）            │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
              ↓ 网络请求（HTTPS）↓
┌─────────────────────────────────────────────────────┐
│         Puter.com 云平台（像饿了么的服务器）        │
│  ┌──────────────┐  ┌──────────────┐              │
│  │   认证服务    │  │   文件存储     │              │
│  │  (身份验证)   │  │  (PDF保存)    │              │
│  └──────────────┘  └──────────────┘              │
│  ┌──────────────┐  ┌──────────────┐              │
│  │   KV存储      │  │   AI服务      │              │
│  │  (数据库)    │  │  (Claude分析)  │              │
│  └──────────────┘  └──────────────┘              │
└─────────────────────────────────────────────────────┘
```

**数据流**：
```
用户操作 → React显示 → Zustand状态管理 → Puter SDK → 云平台处理 → 返回结果
```

### 2.2 技术栈说明（每个技术的作用）

| 技术 | 版本 | 作用 | 类比 |
|------|------|------|------|
| **React** | 19.1.0 | UI框架 | 用户界面的"骨骼" |
| **React Router** | 7.5.3 | 页面导航 | 网站的"菜单" |
| **TypeScript** | 5.8.3 | 类型检查 | 防止"输入错误"的检查器 |
| **Zustand** | 5.0.6 | 状态管理 | 记忆用户操作的"大脑" |
| **Tailwind CSS** | 4.1.4 | 样式设计 | 网页的"美化工具" |
| **Puter.js** | v2 | 云服务SDK | 连接到云平台的"网线" |
| **pdfjs-dist** | 5.3.93 | PDF处理 | 读取和显示PDF的"工具" |
| **react-dropzone** | 14.3.8 | 文件上传 | "拖拽上传"的功能 |

### 2.3 前后端交互图

```
前端（你的浏览器）                后端（Puter云平台）
      │                                  │
      │─── 用户输入简历 ────────────────→│
      │                                  │ 接收并保存
      │                                  │
      │─── 请求AI分析 ─────────────────→│
      │                                  │ 调用Claude AI
      │                                  │
      │←─── 返回分析结果 ────────────────│
      │                                  │
      │─── 用户查看结果 ─────────────────→│
      │                                  │ 检索之前的数据
      │                                  │
      │←─── 返回历史简历 ────────────────│
```

### 2.4 数据流图（简历分析的完整过程）

```
用户 → 上传PDF → 转换为图片 → 保存到云端 → AI分析
 ↓                                              ↓
选择公司和职位 → 提供职位描述 → 等待AI处理 → 获得评分和建议
 ↓
查看详细反馈 → 下载报告 → 修改简历 → 重新上传
```

---

## 3. 数据库设计

### 3.1 存储架构说明（Puter KV + FS）

这个系统**不使用传统数据库**（如MySQL或MongoDB）。相反，它使用Puter平台提供的两种存储方式：

**类比：你的个人云盘**

想象每个用户都有一个私人云盘（就像百度网盘）：
- **文件夹**（File System）- 存放PDF和图片
- **笔记本**（Key-Value Store）- 存放简历的信息和分数

```
你的Puter账户 (私人云空间)
├─ 文件系统
│  ├─ resume_001.pdf         (原始简历)
│  ├─ resume_001_preview.png (缩略图)
│  ├─ resume_002.pdf
│  └─ resume_002_preview.png
│
└─ KV存储（像一个数据表）
   ├─ resume:abc123 → {id, 公司名, 职位, 评分...}
   ├─ resume:def456 → {id, 公司名, 职位, 评分...}
   └─ resume:ghi789 → {id, 公司名, 职位, 评分...}
```

### 3.2 Resume数据模型详解

每个简历包含以下信息：

```typescript
interface Resume {
  id: string;                    // 唯一编号（UUID）
  companyName: string;           // 目标公司（如"Google"）
  jobTitle: string;              // 职位名称（如"Frontend Engineer"）
  jobDescription: string;        // 职位描述
  resumePath: string;            // PDF文件在云端的路径
  imagePath: string;             // 缩略图在云端的路径
  feedback: Feedback;            // AI分析的评分和建议
  createdAt?: string;            // 创建时间
}

interface Feedback {
  overallScore: number;          // 总分（0-100）
  ATS: ScoreSection;             // ATS兼容性评分
  toneAndStyle: ScoreSection;    // 语气和风格
  content: ScoreSection;         // 内容质量
  structure: ScoreSection;       // 结构组织
  skills: ScoreSection;          // 技能匹配
}

interface ScoreSection {
  score: number;                 // 分项得分（0-100）
  tips: Tip[];                   // 改进建议列表（3-4条）
}

interface Tip {
  type: "good" | "improve";      // 类型：优点或改进点
  tip: string;                   // 建议标题
  explanation: string;           // 详细说明
}
```

### 3.3 键命名规范

**KV存储的键**遵循一个简单的规则：

```
格式：resume:{UUID}

示例：
resume:a1b2c3d4-e5f6-7890-abcd-ef1234567890
resume:f4e3d2c1-b0a9-8765-4321-0fedcba98765
```

**为什么这样设计？**
- **resume:** 前缀帮助分类（以后可以扩展，如 `user:`, `company:` 等）
- **UUID** 确保每个简历有唯一的ID，不会重复

**代码位置**: `app/routes/upload.tsx:45`

### 3.4 存储关系图

```
┌──────────────────────────────┐
│      Resume (KV Store)       │
│  {简历元数据}                 │
├──────────────────────────────┤
│ id: UUID                     │
│ companyName: "Google"        │
│ jobTitle: "Engineer"         │
│ resumePath: "/path/1.pdf" ──┐│
│ imagePath: "/path/1.png"  ──┼─┐
│ feedback: { ... }        │  │ │
└──────────────────────────│──│─┘
                           │  │
                ┌──────────▼──▼────────┐
                │  File System (FS)    │
                ├──────────────────────┤
                │ /path/1.pdf          │
                │ /path/1.png          │
                └──────────────────────┘
```

---

## 4. 核心业务流程

### 4.1 用户认证流程图（ASCII）

```
┌─────────────────────────┐
│   应用启动              │
│  (用户打开网站)         │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│  检查用户是否登录        │
│  (自动进行)             │
└────────┬────────────────┘
         │
    ┌────┴────┐
    │          │
    ▼          ▼
┌─────────┐  ┌──────────┐
│ 已登录  │  │ 未登录    │
│ ✓      │  │ ✗        │
└────┬────┘  └────┬─────┘
     │            │
     │            ▼
     │       ┌──────────────┐
     │       │ 跳转到登录页  │
     │       │ /auth        │
     │       └────┬─────────┘
     │            │
     │            ▼
     │       ┌──────────────┐
     │       │ 用户点击登录  │
     │       │ (打开Puter   │
     │       │  登录窗口)   │
     │       └────┬─────────┘
     │            │
     │            ▼
     │       ┌──────────────┐
     │       │ 输入账号密码  │
     │       │ (在Puter中)  │
     │       └────┬─────────┘
     │            │
     │            ▼
     │       ┌──────────────┐
     │       │ Puter验证成功 │
     │       └────┬─────────┘
     │            │
     └────┬───────┘
          │
          ▼
    ┌──────────────────┐
    │ 进入首页(/home)   │
    │ 显示简历列表      │
    └──────────────────┘
```

**关键代码位置**: `app/lib/puter.ts:119-166` (认证逻辑)

### 4.2 简历上传与分析流程（分步骤）

**第1步：用户填写表单**
```
用户选择PDF文件 ──→ 输入目标公司名 ──→ 输入招聘职位 ──→ 输入职位描述
```

**第2步：上传文件**
```
┌─────────────────────────────────────────┐
│ 上传简历PDF                              │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│ 转换PDF为图片（缩略图）                   │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│ 上传图片到云端                           │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│ 保存简历信息到KV存储                     │
│ (此时评分为空)                          │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│ 调用AI开始分析                           │
│ (Claude自动审读简历)                    │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│ AI返回分析结果（评分和建议）              │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│ 更新简历信息（加入评分和建议）            │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│ 跳转到结果页面                           │
│ (显示详细分析)                          │
└─────────────────────────────────────────┘
```

**代码位置**: `app/routes/upload.tsx:21-64`

### 4.3 简历列表加载流程

```
用户打开首页 ──→ 应用从云端查询所有简历 ──→ 解析数据 ──→ 生成卡片显示
```

**具体步骤**：
1. 请求 `resume:*` 的所有数据（"*"是通配符，表示所有以resume:开头的键）
2. 获得一个简历列表
3. 为每个简历加载缩略图
4. 按最新优先排序
5. 显示在屏幕上

**代码位置**: `app/routes/home.tsx:26-40`

### 4.4 简历详情查看流程

```
┌──────────────────────────┐
│ 用户点击某个简历卡片     │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ 从云端加载简历ID         │
└────────┬─────────────────┘
         │
    ┌────┴──────┬────────────┐
    │            │            │
    ▼            ▼            ▼
┌─────────┐ ┌─────────┐ ┌──────────┐
│ 加载PDF │ │ 加载图片 │ │ 加载评分  │
│ (全文)  │ │(预览图) │ │(分数详情)│
└────┬────┘ └────┬────┘ └────┬─────┘
     │           │            │
     └───┬───────┴────┬───────┘
         │            │
         ▼            ▼
    ┌──────────────────────────┐
    │ 并排显示在屏幕           │
    │ 左：PDF                  │
    │ 右：分析报告             │
    └──────────────────────────┘
```

**代码位置**: `app/routes/resume.tsx:25-50`

### 4.5 数据清除流程

```
┌──────────────────────────────────┐
│ 用户点击"清除所有数据"按钮       │
└────────┬───────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ 显示确认对话框                    │
│ ("确定要删除吗?")                │
└────────┬───────────────────────┘
         │
    ┌────┴────┐
    │          │
    ▼          ▼
┌──────────┐  ┌────────────────┐
│ 用户取消 │  │ 用户确认删除     │
│ 返回    │  │               │
└──────────┘  └────┬──────────┘
                   │
                   ▼
          ┌──────────────────────────┐
          │ 1. 删除所有文件            │
          │    (PDF和图片)           │
          └────┬─────────────────────┘
               │
               ▼
          ┌──────────────────────────┐
          │ 2. 删除所有数据            │
          │    (KV存储)              │
          └────┬─────────────────────┘
               │
               ▼
          ┌──────────────────────────┐
          │ 3. 完成                   │
          │ (账户恢复空白状态)       │
          └──────────────────────────┘
```

**代码位置**: `app/routes/wipe.tsx:25-31`

---

## 5. 前后端交互

### 5.1 Puter.js SDK集成说明

**什么是Puter.js？**

想象它像"微信支付"一样的嵌入式系统——你不需要自己写支付代码，只需要调用几个API。

**SDK加载过程**：

```html
<!-- 在 app/root.tsx:44 -->
<script src="https://js.puter.com/v2/"></script>
```

这一行代码会：
1. 从Puter服务器下载SDK代码
2. 在浏览器全局作用域创建 `window.puter` 对象
3. 暴露认证、文件、KV、AI等功能

**SDK初始化**：
```typescript
// app/root.tsx:32
init()  // 调用初始化函数
```

### 5.2 Zustand状态管理

**Zustand是什么？**

类比：你的"大脑"用来记住：
- 当前用户是谁
- 已经上传了哪些简历
- 当前是否在加载中

**代码位置**: `app/lib/puter.ts:45-97`

**使用方法**：
```typescript
// 在任何React组件中
import { usePuterStore } from '~/lib/puter'

const MyComponent = () => {
  const { auth, fs, kv, ai } = usePuterStore()

  // auth.isAuthenticated - 用户是否登录
  // fs.upload() - 上传文件
  // kv.get(key) - 获取数据
  // ai.feedback() - 调用AI
}
```

### 5.3 文件系统操作

**上传文件**：
```typescript
const file = selectedFile  // 用户选择的PDF

const uploaded = await fs.upload([file])
// 返回: { id, name, path, ... }
// path 类似: "/home/user123/uploaded-file.pdf"
```

**读取文件**：
```typescript
const blob = await fs.read(filePath)
// blob 是二进制数据，可以用来显示或下载
```

**删除文件**：
```typescript
await fs.delete(filePath)
// 文件从云端删除
```

**代码位置**: `app/lib/puter.ts:268-311`

### 5.4 KV存储操作

**保存数据**：
```typescript
const data = {
  id: 'abc123',
  companyName: 'Google',
  jobTitle: 'Engineer',
  feedback: { score: 85, ... }
}

await kv.set(
  `resume:abc123`,           // 键名
  JSON.stringify(data)        // 值（必须是字符串）
)
```

**读取数据**：
```typescript
const json = await kv.get('resume:abc123')
const data = JSON.parse(json)
// data 现在包含简历信息
```

**列出所有数据**：
```typescript
const allResumes = await kv.list('resume:*', true)
// 返回所有以 "resume:" 开头的键值对
```

**删除所有数据**：
```typescript
await kv.flush()
// 清空整个KV存储
```

**代码位置**: `app/lib/puter.ts:366-412`

### 5.5 AI服务调用

**AI做什么？**

AI（Claude）可以：
1. 阅读你上传的简历PDF
2. 理解职位需求
3. 分析简历的匹配度
4. 给出具体的改进建议

**调用方式**：
```typescript
const feedback = await ai.feedback(
  resumePath,           // 简历在云端的路径
  instructions          // AI的指示（下面详解）
)

// feedback 返回结果，格式如：
// {
//   message: {
//     content: '{"overallScore": 75, "ATS": {...}, ...}'
//   }
// }
```

**提示词（Instructions）**：
```typescript
const instructions = `You are an expert in ATS (Applicant Tracking System) and resume analysis.
Please analyze this resume for the position: ${jobTitle}
Job description: ${jobDescription}

Rate the resume 0-100 on these dimensions:
1. ATS Compatibility - 是否能通过自动筛选系统
2. Tone & Style - 语气和风格是否专业
3. Content - 内容质量和完整性
4. Structure - 结构和排版
5. Skills - 技能与职位的匹配度

Provide detailed feedback and improvement suggestions.
Return as JSON object with this structure: {...}
`
```

**代码位置**: `app/lib/puter.ts:330-354`

---

## 6. API接口文档（Puter服务）

### 6.1 认证相关API

#### 6.1.1 检查登录状态
**功能**: 检查用户是否已经登录
```typescript
const isLoggedIn = await puter.auth.isSignedIn()
// 返回: true 或 false
```

**使用场景**: 应用启动时自动检查用户状态

**代码位置**: `app/lib/puter.ts:129`

---

#### 6.1.2 获取当前用户信息
**功能**: 获取登录用户的详细信息
```typescript
const user = await puter.auth.getUser()
// 返回: {
//   id: "user_123",
//   username: "john_doe",
//   email: "john@example.com"
// }
```

**使用场景**: 获取用户ID来存储用户特定的数据

**代码位置**: `app/lib/puter.ts:131`

---

#### 6.1.3 打开登录窗口
**功能**: 让用户通过Puter的登录界面登录
```typescript
await puter.auth.signIn()
// 打开一个弹出窗口，用户输入账号密码
// 返回后，可以调用 getUser() 获取用户信息
```

**使用场景**: 用户点击"登录"按钮时

**代码位置**: `app/lib/puter.ts:148`

---

#### 6.1.4 登出
**功能**: 让用户退出登录
```typescript
await puter.auth.signOut()
// 用户会被注销
```

**使用场景**: 用户点击"退出"按钮时

**代码位置**: `app/lib/puter.ts:169`

---

### 6.2 文件系统API

#### 6.2.1 上传文件
**功能**: 将本地文件上传到云端
```typescript
const uploaded = await puter.fs.upload([file1, file2])
// 参数：File 对象数组
// 返回：{ id, name, path, type, size, ... }
```

**参数说明**:
- `file`: JavaScript File 对象（从 `<input type="file">` 获取）

**返回值示例**:
```javascript
{
  id: "file_abc123",
  name: "resume.pdf",
  path: "/home/user_123/resume.pdf",
  type: "application/pdf",
  size: 245000,  // 字节
  modified: "2025-11-16T10:30:00Z"
}
```

**使用场景**: 用户上传简历PDF和预览图

**代码位置**: `app/lib/puter.ts:295-302`

---

#### 6.2.2 读取文件
**功能**: 从云端下载文件内容
```typescript
const blob = await puter.fs.read(filePath)
// 参数：文件在云端的路径（字符串）
// 返回：Blob 对象（二进制数据）
```

**使用示例**:
```typescript
const resumeBlob = await puter.fs.read('/home/user_123/resume.pdf')

// 方式1：显示在网页上
const url = URL.createObjectURL(resumeBlob)
document.getElementById('pdfViewer').src = url

// 方式2：下载到本地
const a = document.createElement('a')
a.href = url
a.download = 'resume.pdf'
a.click()
```

**代码位置**: `app/lib/puter.ts:286-293`

---

#### 6.2.3 列出目录中的文件
**功能**: 获取某个目录下的所有文件
```typescript
const files = await puter.fs.readdir(dirPath)
// 参数：目录路径（通常是 "./")
// 返回：FSItem 数组
```

**返回值示例**:
```javascript
[
  {
    id: "file_1",
    name: "resume.pdf",
    path: "/resume.pdf",
    type: "file"
  },
  {
    id: "file_2",
    name: "photo.jpg",
    path: "/photo.jpg",
    type: "file"
  }
]
```

**使用场景**: 在"清除所有数据"页面列出用户的所有文件

**代码位置**: `app/lib/puter.ts:277-284`

---

#### 6.2.4 删除文件
**功能**: 从云端删除一个文件
```typescript
await puter.fs.delete(filePath)
// 参数：要删除的文件路径
// 返回：undefined（成功则无返回）
```

**使用场景**: 清除所有数据时

**代码位置**: `app/lib/puter.ts:304-311`

---

### 6.3 KV存储API

#### 6.3.1 设置键值对
**功能**: 保存数据到KV存储
```typescript
await puter.kv.set(key, value)
// 参数1：键名（字符串）
// 参数2：值（必须是字符串）
```

**完整示例**:
```typescript
const resumeData = {
  id: 'abc123',
  companyName: 'Google',
  jobTitle: 'Frontend Engineer',
  feedback: {
    overallScore: 85,
    ATS: { score: 90, tips: [...] }
  }
}

await puter.kv.set(
  'resume:abc123',
  JSON.stringify(resumeData)
)
```

**代码位置**: `app/lib/puter.ts:375-382`

---

#### 6.3.2 获取键值对
**功能**: 从KV存储读取数据
```typescript
const value = await puter.kv.get(key)
// 参数：键名
// 返回：值（字符串）或 null（如果键不存在）
```

**完整示例**:
```typescript
const json = await puter.kv.get('resume:abc123')

if (json) {
  const data = JSON.parse(json)
  console.log(data.companyName)  // "Google"
} else {
  console.log('简历不存在')
}
```

**代码位置**: `app/lib/puter.ts:366-372`

---

#### 6.3.3 列出匹配的键
**功能**: 查找所有匹配模式的键
```typescript
const keys = await puter.kv.list(pattern, returnValues)
// 参数1：匹配模式（如 "resume:*"）
// 参数2：是否返回值（默认 false）
// 返回：键名数组或键值对数组
```

**完整示例**:
```typescript
// 只获取键名
const keyNames = await puter.kv.list('resume:*', false)
// 返回: ['resume:abc123', 'resume:def456', ...]

// 获取键名和值
const items = await puter.kv.list('resume:*', true)
// 返回: [
//   { key: 'resume:abc123', value: '{"id":"abc123",...}' },
//   { key: 'resume:def456', value: '{"id":"def456",...}' }
// ]
```

**代码位置**: `app/lib/puter.ts:393-403`

---

#### 6.3.4 删除键值对
**功能**: 从KV存储删除一个键
```typescript
await puter.kv.delete(key)
// 参数：要删除的键名
```

**代码位置**: `app/lib/puter.ts:384-391`

---

#### 6.3.5 清空所有数据
**功能**: 删除KV存储中的所有键值对
```typescript
await puter.kv.flush()
// 无参数
// 返回：boolean（是否成功）
```

**危险操作**: 这个操作**不可撤销**！使用前必须确认。

**代码位置**: `app/lib/puter.ts:405-412`

---

### 6.4 AI服务API

#### 6.4.1 通用对话API
**功能**: 与AI进行对话
```typescript
const response = await puter.ai.chat(
  prompt,              // 用户问题
  imageURL,           // (可选)图片URL
  testMode,           // (可选)测试模式
  { model: "..." }    // (可选)模型配置
)
// 返回：{ message: { content: "..." } }
```

**代码位置**: `app/lib/puter.ts:313-328`

---

#### 6.4.2 简历分析API（推荐使用）
**功能**: 专门用于分析简历的API
```typescript
const feedback = await puter.ai.feedback(
  resumePath,         // 简历在云端的路径
  instructions        // AI的分析指示
)
// 返回：AI的分析结果
```

**完整示例**:
```typescript
const instructions = `You are an ATS expert.
Analyze this resume for: ${jobTitle}
Job description: ${jobDescription}
Rate on: ATS, Tone, Content, Structure, Skills
Return JSON only.`

const result = await puter.ai.feedback(
  '/home/user123/resume.pdf',
  instructions
)

// result 格式：
// {
//   message: {
//     content: '{"overallScore":85,"ATS":{...},...}'
//   }
// }

const feedback = JSON.parse(result.message.content)
```

**代码位置**: `app/lib/puter.ts:330-354`

---

---

## 7. 环境配置指南

### 7.1 开发环境配置

**第1步：安装Node.js**
```bash
# 检查是否已安装
node --version  # 应该是 v20.x

# 如果没安装，从官网下载：
# https://nodejs.org/  (选择LTS版本)
```

**第2步：克隆项目**
```bash
git clone https://github.com/your-repo/ai-resume-analyzer.git
cd ai-resume-analyzer
```

**第3步：安装依赖**
```bash
npm install
# 这会下载所有需要的包到 node_modules 目录
```

**第4步：启动开发服务器**
```bash
npm run dev
# 输出：
# > Vite v6.3.3 dev server running at:
# > http://localhost:5173
# >
# > CTRL+C to stop

# 在浏览器中打开: http://localhost:5173
```

**开发特性**：
- 💫 **热更新** - 修改代码后自动刷新
- 🔍 **Debug工具** - 浏览器开发者工具可以直接调试
- ⚡ **快速启动** - Vite提供毫秒级的启动速度

**常见问题**：

| 问题 | 解决方法 |
|------|---------|
| `npm: command not found` | 需要先装Node.js |
| `Port 5173 already in use` | 关闭占用该端口的程序，或改用其他端口 `npm run dev -- --port 3000` |
| `module not found` | 运行 `npm install` 重新安装依赖 |

### 7.2 生产环境配置

**第1步：构建项目**
```bash
npm run build
# 这会编译TypeScript，打包代码，优化样式
# 输出到 build/ 目录
```

**第2步：测试生产版本**
```bash
npm run start
# 启动生产服务器：http://localhost:3000
# 这和实际部署环境完全相同
```

**第3步：部署到云端（选择一个）**

**选项A：Docker**
```bash
docker build -t ai-resume-analyzer:v1.0.0 .
docker run -p 3000:3000 ai-resume-analyzer:v1.0.0
```

**选项B：Vercel**
```bash
npm install -g vercel
vercel deploy --prod
```

**选项C：自己的服务器**
```bash
# 将代码复制到服务器
scp -r ai-resume-analyzer/ user@server.com:/opt/

# 在服务器上
cd /opt/ai-resume-analyzer
npm install --production
npm run start &  # 后台运行
```

### 7.3 Docker部署配置

**Dockerfile说明**：

```dockerfile
# 第1阶段：安装依赖
FROM node:20-alpine AS development-dependencies-env
COPY . /app
WORKDIR /app
RUN npm ci

# 第2阶段：安装生产依赖
FROM node:20-alpine AS production-dependencies-env
COPY ./package.json package-lock.json /app/
WORKDIR /app
RUN npm ci --omit=dev  # 不安装开发用工具

# 第3阶段：编译代码
FROM node:20-alpine AS build-env
COPY . /app/
COPY --from=development-dependencies-env /app/node_modules /app/node_modules
WORKDIR /app
RUN npm run build

# 第4阶段：运行时
FROM node:20-alpine
COPY ./package.json package-lock.json /app/
COPY --from=production-dependencies-env /app/node_modules /app/node_modules
COPY --from=build-env /app/build /app/build
WORKDIR /app
CMD ["npm", "run", "start"]
```

**为什么这样设计？**

就像做菜一样：
1. **第1阶段** - 准备所有食材（包括做菜的工具）
2. **第2阶段** - 只准备最终需要的食材（不需要菜谱）
3. **第3阶段** - 做菜（把菜做好）
4. **第4阶段** - 上桌（最后只需要做好的菜，不需要工具和菜谱）

**最终结果**：镜像大小减少了70%！

### 7.4 环境变量说明（为什么不需要配置）

**传统项目需要的环境变量**：
- 数据库连接串
- API密钥
- 服务器地址
- 日志级别

**这个项目**：
- ✅ 身份验证 → Puter自动处理
- ✅ 文件存储 → Puter自动隔离
- ✅ 数据库 → Puter KV存储自动分配
- ✅ AI服务 → Puter平台提供

**唯一可能需要的**：
```bash
# 可选的环境变量（开发时）
NODE_ENV=production     # 生产模式优化
VITE_LOG_LEVEL=error   # 日志级别
```

**如何设置**：
```bash
# Linux/Mac
export NODE_ENV=production

# Windows
set NODE_ENV=production

# Docker
docker run -e NODE_ENV=production ...
```

---

## 8. 二次开发指南

### 8.1 开发环境搭建步骤

**第1步：准备**
```bash
# 确保Node.js已安装
node --version  # v20.x

# 克隆代码
git clone <repo>
cd ai-resume-analyzer

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

**第2步：理解项目结构**
```
app/
├── routes/          ← 页面文件（每个文件是一个页面）
│   ├── home.tsx     （首页 - 简历列表）
│   ├── auth.tsx     （登录页）
│   ├── upload.tsx   （上传页）
│   ├── resume.tsx   （详情页）
│   └── wipe.tsx     （清除数据页）
├── components/      ← 可复用的UI组件
│   ├── Navbar.tsx
│   ├── ResumeCard.tsx
│   └── ...
├── lib/            ← 工具和服务
│   ├── puter.ts    （Puter SDK包装）
│   ├── pdf2img.ts  （PDF转图片）
│   └── utils.ts    （通用工具）
├── root.tsx        ← 根布局
├── routes.ts       ← 路由配置
└── app.css         ← 全局样式
```

### 8.2 项目结构说明

**app/routes/ - 页面层**

每个文件代表一个页面：

```typescript
// app/routes/home.tsx
export default function Home() {
  // 显示简历列表
  return (
    <main>
      <Navbar />
      <ResumeCard resume={resume1} />
      <ResumeCard resume={resume2} />
    </main>
  )
}
```

**app/components/ - 组件层**

可复用的小组件：

```typescript
// app/components/ResumeCard.tsx
export function ResumeCard({ resume }) {
  // 显示单个简历的卡片
  return (
    <div>
      <img src={resume.imagePath} />
      <h3>{resume.companyName}</h3>
      <ScoreCircle score={resume.feedback.overallScore} />
    </div>
  )
}
```

**app/lib/ - 业务逻辑层**

处理数据和API调用：

```typescript
// app/lib/puter.ts
export const usePuterStore = create<PuterStore>((set) => ({
  auth: {
    signIn: () => { /* 调用Puter API */ },
    getUser: () => { /* 从Puter获取用户 */ }
  },
  fs: {
    upload: (files) => { /* 上传文件 */ }
  },
  // ... 其他方法
}))
```

### 8.3 常见开发任务示例

#### 任务1：添加新路由页面

**需求**：添加一个"我的成就"页面（`/achievements`）

**步骤1**：创建页面文件
```typescript
// app/routes/achievements.tsx
export const meta = () => [{ title: 'My Achievements' }]

export default function Achievements() {
  const { auth } = usePuterStore()

  // 添加路由保护
  useEffect(() => {
    if(!auth.isAuthenticated) navigate('/auth?next=/achievements')
  }, [auth.isAuthenticated])

  return (
    <main>
      <h1>My Achievements</h1>
      {/* 你的内容 */}
    </main>
  )
}
```

**步骤2**：注册路由
```typescript
// app/routes.ts
export default [
  index("routes/home.tsx"),
  route('/auth', 'routes/auth.tsx'),
  route('/upload', 'routes/upload.tsx'),
  route('/resume/:id', 'routes/resume.tsx'),
  route('/achievements', 'routes/achievements.tsx'),  // 新增
  route('/wipe', 'routes/wipe.tsx'),
]
```

**步骤3**：添加菜单链接
```typescript
// app/components/Navbar.tsx
<Link to="/achievements">
  <span>🏆 Achievements</span>
</Link>
```

#### 任务2：创建新UI组件

**需求**：创建一个"徽章"组件显示用户成就

**步骤1**：创建组件文件
```typescript
// app/components/AchievementBadge.tsx

interface AchievementBadgeProps {
  title: string
  icon: string
  color: 'gold' | 'silver' | 'bronze'
}

export function AchievementBadge({
  title,
  icon,
  color
}: AchievementBadgeProps) {
  const bgColor = {
    gold: 'bg-yellow-100',
    silver: 'bg-gray-100',
    bronze: 'bg-orange-100'
  }[color]

  return (
    <div className={`p-4 rounded-lg ${bgColor}`}>
      <div className="text-4xl">{icon}</div>
      <p className="mt-2">{title}</p>
    </div>
  )
}
```

**步骤2**：在页面中使用
```typescript
// app/routes/achievements.tsx
import { AchievementBadge } from '~/components/AchievementBadge'

<AchievementBadge
  title="Perfect Score"
  icon="⭐"
  color="gold"
/>
```

#### 任务3：修改AI提示词

**需求**：修改AI的分析标准，增加对工作经验的评估

**步骤1**：找到提示词定义
```typescript
// constants/index.ts:228-241
export const prepareInstructions = ({
  jobTitle,
  jobDescription
}: {
  jobTitle: string
  jobDescription: string
}) => `You are an expert in ATS...`
```

**步骤2**：修改提示词
```typescript
export const prepareInstructions = ({
  jobTitle,
  jobDescription
}: {
  jobTitle: string
  jobDescription: string
}) => `You are an expert in ATS and resume analysis.
Please analyze this resume with special attention to:

1. Work Experience Relevance
   - Years of relevant experience
   - Progression and growth
   - Industry alignment

2. Technical Skills Match
3. Soft Skills
4. ATS Compatibility
5. Document Format

${AIResponseFormat}

Return as JSON object only.`
```

**步骤3**：相应地修改数据模型
```typescript
// types/index.d.ts
interface Feedback {
  overallScore: number
  workExperience: ScoreSection  // 新增
  technicalSkills: ScoreSection
  softSkills: ScoreSection
  ATS: ScoreSection
  // ...
}
```

#### 任务4：调整简历评分规则

**需求**：改变总分的计算方式（目前是平均值，改为加权平均）

**步骤1**：找到评分逻辑
```typescript
// app/lib/puter.ts - 在feedback()之后的处理
const feedback = JSON.parse(feedbackText)
// 当前逻辑：overallScore 已经由AI给出

// 如果要自己计算，可以这样做：
const calculatedScore =
  feedback.ATS.score * 0.3 +           // ATS权重30%
  feedback.content.score * 0.3 +        // 内容权重30%
  feedback.structure.score * 0.2 +      // 结构权重20%
  feedback.skills.score * 0.2            // 技能权重20%

feedback.overallScore = Math.round(calculatedScore)
```

#### 任务5：添加新的反馈维度

**需求**：添加"创意和创新"维度

**步骤1**：更新数据模型
```typescript
// types/index.d.ts
interface Feedback {
  overallScore: number
  ATS: ScoreSection
  toneAndStyle: ScoreSection
  content: ScoreSection
  structure: ScoreSection
  skills: ScoreSection
  creativity: ScoreSection  // 新增
}
```

**步骤2**：更新AI提示词
```typescript
// constants/index.ts
export const AIResponseFormat = `{
  "overallScore": <0-100>,
  "ATS": { "score": <0-100>, "tips": [...] },
  "toneAndStyle": { "score": <0-100>, "tips": [...] },
  "content": { "score": <0-100>, "tips": [...] },
  "structure": { "score": <0-100>, "tips": [...] },
  "skills": { "score": <0-100>, "tips": [...] },
  "creativity": { "score": <0-100>, "tips": [...] }  // 新增
}`
```

**步骤3**：在UI中显示
```typescript
// app/components/Summary.tsx
<ScoreBadge name="Creativity" score={feedback.creativity.score} />
```

### 8.4 调试技巧

**技巧1：使用浏览器开发者工具**
```javascript
// 在浏览器控制台输入
const store = document.querySelector('body').__reactFiber$.child.return.memoizedProps.store
console.log(store.auth.user)  // 查看当前用户
```

**技巧2：添加console.log调试**
```typescript
// app/routes/upload.tsx
const handleAnalyze = async (data) => {
  console.log('Starting upload...', data)
  const uploaded = await fs.upload([data.file])
  console.log('Upload complete:', uploaded)

  const feedback = await ai.feedback(...)
  console.log('AI feedback:', feedback)
}
```

**技巧3：检查Puter API调用**
```typescript
// app/lib/puter.ts - 添加日志
const upload = async (files) => {
  console.log('[DEBUG] Uploading files:', files)
  const result = await puter.fs.upload(files)
  console.log('[DEBUG] Upload result:', result)
  return result
}
```

**技巧4：监控状态变化**
```typescript
// 在组件中
import { usePuterStore } from '~/lib/puter'

const store = usePuterStore()

// 订阅状态变化
store.subscribe((state) => {
  console.log('State changed:', state)
})
```

### 8.5 性能优化建议

**优化1：代码分割（已自动做）**
```typescript
// React Router会自动为每个路由创建分离的JS包
// app/routes/home.tsx  → home.js
// app/routes/upload.tsx → upload.js
// 用户只加载需要的代码
```

**优化2：图片优化**
```typescript
// 在 ResumeCard 中，缩略图使用了lazy loading
// 只在用户滚动到该卡片时才加载图片
<img src={url} loading="lazy" />
```

**优化3：减少API调用**
```typescript
// 不好❌
const list = await kv.list('resume:*')
const list2 = await kv.list('resume:*')  // 重复调用

// 好✅
const list = await kv.list('resume:*')
// 重复使用 list，或缓存结果
const cached = useRef(list)
```

**优化4：PDF处理优化**
```typescript
// pdf2img.ts 已使用懒加载
// pdfjs 库只在第一次需要时才下载
// 之后的调用直接使用缓存的库
```

---

## 9. 部署指南

### 9.1 Docker部署步骤（详细）

**前提条件**：
- 已安装Docker（[下载](https://www.docker.com/products/docker-desktop)）
- 有一个能运行Docker的服务器或个人电脑

**步骤1：构建镜像**
```bash
# 在项目根目录运行
docker build -t ai-resume-analyzer:v1.0.0 .

# 输出示例：
# Sending build context to Docker daemon  120.3MB
# Step 1/15 : FROM node:20-alpine
# ...
# Successfully tagged ai-resume-analyzer:v1.0.0
```

**步骤2：验证镜像**
```bash
# 查看构建的镜像
docker images | grep ai-resume-analyzer

# 输出：
# ai-resume-analyzer       v1.0.0       abc123def456   2 minutes ago   420MB
```

**步骤3：本地测试**
```bash
# 运行容器
docker run -p 3000:3000 ai-resume-analyzer:v1.0.0

# 输出：
# > react-router-serve ./build/server/index.js
# Server running at http://localhost:3000

# 在浏览器打开：http://localhost:3000
```

**步骤4：后台运行（生产环境）**
```bash
# 停止前一个容器
docker stop $(docker ps -q --filter ancestor=ai-resume-analyzer:v1.0.0)

# 后台运行新容器
docker run -d \
  --name my-resume-app \
  -p 3000:3000 \
  -e NODE_ENV=production \
  ai-resume-analyzer:v1.0.0

# 查看容器状态
docker ps

# 查看日志
docker logs my-resume-app

# 如果需要停止
docker stop my-resume-app
docker rm my-resume-app
```

**步骤5：持久化数据（可选）**
```bash
# 如果需要保存上传的文件等
docker run -d \
  --name my-resume-app \
  -p 3000:3000 \
  -v /local/path:/app/data \
  ai-resume-analyzer:v1.0.0
```

### 9.2 Vercel部署步骤

**Vercel是什么？** - 一个自动化部署平台，一键将代码部署到全球CDN

**步骤1：创建Vercel账户**
- 访问 [vercel.com](https://vercel.com)
- 用GitHub账户登录

**步骤2：连接GitHub项目**
- 点击"New Project"
- 选择你的GitHub仓库
- 选择"ai-resume-analyzer"项目

**步骤3：配置项目**
```json
// vercel.json (在项目根目录创建)
{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "functions": {
    "build/server/index.js": {
      "memory": 512,
      "maxDuration": 60
    }
  }
}
```

**步骤4：部署**
```bash
# 方式1：通过网页界面
# 在Vercel网站点击"Deploy"按钮

# 方式2：命令行
npm install -g vercel
vercel login
vercel deploy --prod
```

**步骤5：监控部署**
- Vercel网站显示部署进度
- 部署完成后获得 URL（如 `https://ai-resume-analyzer.vercel.app`）

### 9.3 其他平台部署建议

**AWS Elastic Beanstalk**
```bash
# 安装EB CLI
pip install awsebcli

# 初始化项目
eb init -p node.js-20 ai-resume-analyzer

# 部署
eb create production
eb deploy
```

**Heroku**
```bash
# 登录
heroku login

# 创建应用
heroku create ai-resume-analyzer

# 部署
git push heroku main

# 查看日志
heroku logs --tail
```

**自己的Linux服务器**
```bash
# 上传文件
scp -r . user@server.com:/opt/ai-resume-analyzer

# 连接服务器
ssh user@server.com

# 安装依赖并启动
cd /opt/ai-resume-analyzer
npm install --production
npm run start

# 使用 PM2 保持运行
npm install -g pm2
pm2 start "npm start" --name "resume-app"
pm2 startup
pm2 save
```

### 9.4 环境配置

**关键的环境变量**：
```bash
# .env.production
NODE_ENV=production
PORT=3000
```

**Docker环境**：
```bash
docker run \
  -e NODE_ENV=production \
  -e PORT=3000 \
  -p 3000:3000 \
  ai-resume-analyzer:v1.0.0
```

**Vercel环境**：
- 在Vercel网站的"Settings" → "Environment Variables"中配置

### 9.5 常见问题排查

| 问题 | 症状 | 解决方法 |
|------|------|---------|
| 构建失败 | `npm ERR! ...` | 运行 `npm ci --force` 重新安装 |
| 容器无法启动 | `docker: Error response from daemon` | 检查端口是否已占用 |
| 内存不足 | 应用频繁崩溃 | 增加容器内存：`docker run -m 1g ...` |
| Puter.js未加载 | 登录功能不工作 | 检查网络连接和Puter.js CDN是否可用 |
| PDF转换失败 | 上传后没有缩略图 | 检查文件是否是有效的PDF格式 |
| 文件上传很慢 | 上传时间很长 | 检查网络速度，或使用更小的文件 |

---

## 10. 附录

### 10.1 技术术语表（简单解释）

| 术语 | 解释 | 类比 |
|------|------|------|
| **API** | 应用程序接口，让程序之间互相交流 | 餐厅的菜单（告诉你能做什么） |
| **SDK** | 软件开发工具包，提供现成的功能 | 搭积木用的积木集合 |
| **KV存储** | 键值存储，像字典一样存数据 | 你的通讯录（名字→电话） |
| **Blob** | 二进制大对象，用来存文件内容 | 装数据的"盒子" |
| **UUID** | 全球唯一标识符，保证不重复 | 身份证号 |
| **SSR** | 服务端渲染，在服务器生成HTML | 厨房做好饭送到桌子上 |
| **CSR** | 客户端渲染，在浏览器生成HTML | 面条端到桌子上再装盘 |
| **缓存** | 存储常用数据以加快速度 | 在办公桌上放最常用的文件 |
| **Zustand** | React状态管理库 | 记忆用户操作的"小本子" |
| **Vite** | 快速的前端构建工具 | 编译代码的"加速器" |
| **Docker** | 容器化部署工具 | 把应用装在"集装箱"里运输 |

### 10.2 常用命令速查

**开发命令**：
```bash
npm run dev          # 启动开发服务器（热更新）
npm run build        # 编译生产版本
npm run start        # 启动生产服务器
npm run typecheck    # 检查TypeScript错误
```

**Git命令**：
```bash
git clone ...        # 克隆项目
git add .            # 添加文件到暂存区
git commit -m "..."  # 提交更改
git push             # 推送到远程
git pull             # 拉取更新
```

**Docker命令**：
```bash
docker build -t app:v1 .           # 构建镜像
docker run -p 3000:3000 app:v1     # 运行容器
docker logs <id>                   # 查看日志
docker exec -it <id> bash          # 进入容器
```

### 10.3 学习资源推荐

**官方文档**：
- [React官方](https://react.dev) - React框架
- [React Router文档](https://reactrouter.com) - 路由管理
- [Puter.js文档](https://docs.puter.com) - 云平台API
- [TypeScript手册](https://www.typescriptlang.org/docs) - 类型系统

**中文学习资源**：
- [React中文文档](https://zh-hans.react.dev) - React入门
- [ES6教程](https://wangdoc.com/es6/) - JavaScript新特性
- [Tailwind CSS中文文档](https://www.tailwindcss.cn) - 样式框架

**视频教程**：
- YouTube: "React Router v7 Tutorial"
- Bilibili: "React 完整教程"

### 10.4 常见问题FAQ

**Q: 我可以离线使用这个应用吗？**
A: 不行。因为需要连接Puter云平台进行认证、存储和AI分析。

**Q: 我的简历数据会被保存多久？**
A: 取决于你的Puter账户。你随时可以删除，使用"清除所有数据"功能。

**Q: 可以修改AI的分析结果吗？**
A: 目前不行。但你可以修改简历后重新上传来获得新的评分。

**Q: 怎样才能扩展这个项目？**
A: 按照"二次开发指南"（第8章）的步骤添加新功能。

**Q: 部署到生产环境需要多少成本？**
A: 取决于平台：
- Vercel: 免费用量充足，超出后按量付费
- Docker + VPS: 每月 $5-20（服务器成本）
- AWS: 按使用量计费，通常 $10-50/月

**Q: 如何处理用户隐私？**
A: 所有数据都存储在用户的Puter账户，平台无法访问。建议添加隐私政策。

**Q: 支持多少个简历同时存储？**
A: 理论上无限制，取决于Puter账户的存储空间。

### 10.5 项目文件索引

**核心文件位置速查**：

| 需要修改 | 文件路径 |
|---------|---------|
| 添加新页面 | `app/routes/[newpage].tsx` |
| 添加新组件 | `app/components/[NewComponent].tsx` |
| 修改样式 | `app/app.css` 或在组件中用Tailwind |
| 修改路由 | `app/routes.ts` |
| 修改AI提示词 | `constants/index.ts:228-241` |
| 修改数据模型 | `types/index.d.ts` |
| 调整认证 | `app/lib/puter.ts:119-213` |
| 部署配置 | `Dockerfile` 或 `vercel.json` |

---

## 总结

这个项目展示了现代Web应用的最佳实践：

✅ **技术亮点**：
- 使用Serverless架构，无需管理服务器
- React Router v7 提供最新的路由方案
- TypeScript确保代码质量
- Tailwind CSS快速构建UI
- Puter平台提供完整的云服务

✅ **开发体验**：
- 热更新支持，开发效率高
- 清晰的项目结构，易于维护
- 完整的类型提示，减少错误
- 丰富的文档和示例

✅ **生产就绪**：
- Docker多阶段构建，镜像优化
- 支持多种部署平台
- 自动代码分割和资源优化
- 完整的错误处理和日志

**推荐流程**：
1. 先理解整个架构（第2-3章）
2. 按照开发指南搭建环境（第7-8章）
3. 试着做一个小功能增强（参考第8.3章）
4. 最后根据需要部署（第9章）

祝你开发愉快！如有问题，欢迎查阅各章节的详细说明。

---

**文档版本**: 1.0
**最后更新**: 2025-11-16
**总字数**: 约18,000字
**适用版本**: v1.0.0 及以上
